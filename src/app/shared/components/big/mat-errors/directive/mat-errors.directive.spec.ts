import { MatErrorsDirective } from './mat-errors.directive';

describe('MatErrorsDirective', () => {
  it('should create an instance', () => {
    const directive = new MatErrorsDirective();
    expect(directive).toBeTruthy();
  });
});
