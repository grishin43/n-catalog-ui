export enum ToolbarPluginEnum {
  SCHEME_VALIDATOR = 'schemeValidator',
  TOKEN_SIMULATION = 'tokenSimulation',
  TRANSACTION_BOUNDARIES = 'transactionBoundaries',
  EMBEDDED_COMMENTS = 'embeddedComments'
}
